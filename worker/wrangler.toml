name = "portal-auth"
main = "src/index.ts"
compatibility_date = "2024-09-23"

[[d1_databases]]
binding = "DB"
database_name = "portal-users"
database_id = "c9196fc8-bdd7-4b12-b4c4-10621db0ad31" 
migrations_dir = "migrations"

# JWT_SECRET is a secret — never put the real value here.
# Set it via: wrangler secret put JWT_SECRET

[dev]
port = 8787
